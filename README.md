# WorkWell AI

Web app cảnh báo dáng ngồi: MediaPipe Pose chạy trên trình duyệt, không gửi video lên server.

```bash
cd workwell-ai
npm run dev
```

Mở http://localhost:3000 → bật camera → cho phép quyền webcam.
